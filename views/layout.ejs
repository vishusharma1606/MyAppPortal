<!doctype html>
<html>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title><%= title || 'Simple Auth App' %></title>
    <link rel="stylesheet" href="/style.css" />
  </head>
  <body>
    <header>
      <h1>Simple Auth App</h1>
      <% if (user) { %>
        <div class="nav">Welcome, <strong><%= user %></strong> — <a href="/logout">Logout</a></div>
      <% } %>
    </header>
    <main>
      <%- body %>
    </main>
  </body>
</html>
